/**
 * If you ever need something that is part of the runtime, not the language
 * (e.g. DOM methods, setTimeout, process.nextTick), get the global from this
 * module.
 */

/* global global */
export {global}

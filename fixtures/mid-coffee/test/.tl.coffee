'use strict'

t = require 'thallium'
t.reporter require '../../../helpers/pipe-reporter.js'

# It's a good place to test reporters, too ;-)
# Just remember to comment out the pipe-reporter reporter for testing above, and
# to not commit the changes.
# t.reporter require('../../../r/spec.js')()

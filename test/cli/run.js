"use strict"

// var path = require("path")

// var t = require("../../index.js")
// var run = require("../../../lib/cli/run.js").run

describe("cli config (infer location)", () => {
    // TODO: fill this in with sanity tests.
    // Note the function prototypes:
    //
    // class State(opts: {argv, cwd, util})
    // run(state: State): Promise<void>
    // exitReporter(state: State): (x: TestReport<TestType, any>) => void
})
